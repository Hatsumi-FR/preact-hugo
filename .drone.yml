---
kind: pipeline
type: docker
name: un-nom

steps:
  - name: preact-app
    image: lowess/preact-cli
    commands:
      - preact create ${DRONE_REPO}#${DRONE_COMMIT} app
      - cd app
      - npm install
      - npm run build
  - notify:
      image: plugins/slack
      webhook: https://hooks.slack.com/services/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
      channel: drone-cli
      username: Gopher
      template: >
        {{#success build.status}}
        {{/success}}
